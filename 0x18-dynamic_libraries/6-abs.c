#include "main.h"
/**
 *  _abs - that computes the absolute of an integer
 *  @f: The integer to be completed
 *  Return: The absolute value
 */
int _abs(int f)
{
	if (f >= 0)
		return (f);
	else
		return (-f);
}
